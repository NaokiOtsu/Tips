(function(window) {
  'use strict';

  function FigureDetailView() {
    this.$container = $('#figure-detail');
  }

  window.app = window.app || {};
  window.app.FigureDetailView = FigureDetailView;
})(window);